# Frontend environment variables for local development
# Base URL for the FastAPI backend
VITE_API_BASE_URL=http://localhost:8000

# Optional admin key for calling protected /history and /stats endpoints
# Do NOT put real secrets in VITE_* variables for production clients.
VITE_ADMIN_API_KEY=
